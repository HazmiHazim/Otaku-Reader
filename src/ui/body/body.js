import React from "react";
import { StyleSheet } from "react-native";

const body = StyleSheet.create({
    layout: {
        display: 'flex',
        flex: 1,
        position: 'relative',
        margin: 0,
        padding: 0,
        width: '100%',
        height: '100%',
    },
});

export default body;